using UnityEngine;
using System.Collections;

namespace AceViral {
    /// <summary>
    /// AV scale type. Used to identify the dominant orientation when scaling equally.
    /// </summary>
    public enum ScaleType {
    	Width,
    	Height,
    	Both,
    	NoScale
    }
}
